# Faisal Alsurayhi — Analytics Portfolio

A responsive, accessible one-page portfolio built with plain HTML, CSS, and JavaScript.

## Local preview

Open `index.html` in a browser or run a local static server.

## Publishing

The site is compatible with GitHub Pages and requires no build step.
